<%= partial 'system/edit', :obj => @world_region %>

  <!-- Little form just for the delete button: -->
  <%#= delete_button(@world_region, "Delete #{@world_region.name}") %>
  <%#= link_to 'Cancel without saving', resource(:world_regions) %>
