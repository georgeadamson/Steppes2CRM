<% label = note.new? ? 'New note' : 'Edit note' %>
<%= hidden_field :client_id %>
<%= text_area :name, :label => label, :class => 'width2Fields', :maxlength => 200, :placeholder => "You'll end up with a blank note unless you type something here you numpty" %>