
<h3>Change attributes of this photo</h3>

<%= form_for(@photo, :action => resource(@photo) ) do %>

  <%= partial 'photos/fields', :photo => @photo %>

  <%= submit "Save", :class=>:ajax %>
  <%= link_to 'Cancel without saving', resource(:photos) %>

<% end =%>

  <!-- Little form just for the delete button: -->
  <%#= delete_button(@article, "Delete #{@article.name}") %>
  
