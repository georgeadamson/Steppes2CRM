
<h3>Change <%= @article.articleType.name %> for <%= @country.name %></h3>

<%= form_for(@article, :action => resource(@article) ) do %>

  <%= partial 'articles/fields', :article => @article %>
  <%= submit "Save" %>

<% end =%>

  <!-- Little form just for the delete button: -->
  <%#= delete_button(@article, "Delete #{@article.name}") %>
  <%= link_to 'Cancel without saving', resource(:articles) %>

