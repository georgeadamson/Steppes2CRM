<%

	# If a list of template filename <option> items is being requested then pass the job to a partial:
	if params[:list] == 'option'
	
		return partial 'documents/list', 
			:type				=> params[:type], 
			:prefix			=> params[:prefix], 
			:company_id => params[:company_id]
	
	end

%>

<%= partial 'documents/index', :documents => @documents %>