<%

	task_type ||= @task_type
	@properties = task_type.method(:properties).call

	@show_all_required_field_message = true
	
%>
<%= text_field :name, :label => 'Task type', :maxlength => @properties[:name].length, :class => 'width2Fields' %>

