<%=

	# Return a summary of a client's details.
	# Typically used for building a mailto link with detailed body parameter.
	# Used on the trip page to provide mailto's with client's details.
	# Expects parameter "client".
	
	# Default to use a mailto-friendly linefeed character:
	new_line ||= '%0A'

	# Format the output by separating items by the new_line character:
	[

		'Details of client:',
		client.fullname,
		"DOB: #{ h client.birth_date.formatted(:uidisplay) }",
		"Age: #{ client.age }",
		"Name on passport: #{ h client.passport_name }",
		"Passport expires: #{ h client.passport_expiry_date.formatted(:uidisplay) }",
		"Passport issue date: #{ h client.passport_issue_date.formatted(:uidisplay) }",
		"Passport number: #{ h client.passport_number }",
		"Airline number: #{ h client.notes_frequent_flyer }",
		"Airline preference: #{ h client.notes_airline }",
		"Seat preference: #{ h client.notes_airline }",
		"Meal preference: #{ h client.notes_food }",
		"General preference: #{ h client.notes_general }",
		"Nationality: #{ h client.nationality }",
		"Occupation: #{ h client.occupation }"

	].join(new_line)

%>