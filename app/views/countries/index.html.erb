<%

	# Return some sort of list of countries.
	# Delegate the task to specialised partials, depending on parameters.

	# TODO: Refactor using fetch_partial to cache the html.

	# When url parameter ?list=option is set then return custom list:
	return partial( 'countries/list', :countries => @countries ) if params[:list]

	# Otherwise return a list for use in the system admin page:

-%>
<%= partial 'system/index', :collection => @countries %>