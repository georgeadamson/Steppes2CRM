<%

	# When url parameter ?list=option is set then return custom list:
	return partial( 'document_types/list', :document_types => @document_types ) if params[:list]

-%>
<%= partial 'system/index', :collection => @document_types %>