
<h3>New image</h3>

<%= form_for(@image, :action => resource(:images) ) do %>

  <%= partial 'images/fields', :image => @image %>
  <br/>
  <%= submit "Save" %>

<% end =%>

  <!-- Little form just for the delete button: -->
  <%#= delete_button(@article, "Delete #{@article.name}") %>
  <%= link_to 'Cancel without saving', resource(:images) %>
